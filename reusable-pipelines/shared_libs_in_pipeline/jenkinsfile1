
@Library('Jenkins_Pipeline_Shared_Libs@main') _
pipeline {
    agent any

    stages {
        stage('Audit tools') {
            steps {
                script {
                    // Call shared library function
                    auditTools()
                }
            }
        }
    }
}

